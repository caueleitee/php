Precisamos criar um repositório local, adicionar um arquivo nele e atualizar esse arquivo em nosso repositório remoto. Qual série de comandos atenderia essa necessidade?

git init
git remote add origin https://endereco_do_repositorio
git add --all
git commit -m 'mensagem'
git push origin master


git add --all
git commit -m 'mensagem'

git pull origin master : Pega os dados do master e traz para o local

git push origin master : Envia as informações do remoto para o master

